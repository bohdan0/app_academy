<h2>Our cats!</h2>
<ul>
  <% @cats.each do |cat| %>
    <li><%= link_to "#{cat.name}", cat_url(cat.id) %></li>
  <% end %>
</ul>
<%= link_to "Add a cat!", new_cat_url %>
<%= link_to "Rent a cat!", new_cat_rental_request_url %>
